import { OrganismPresets } from "@madnesslabs/fireenjin-designer/dist/types/interfaces";

export default {
  default: {
    name: "Default",
    props: {
      src: null,
      initials: "TT",
    },
  },
} as OrganismPresets;
